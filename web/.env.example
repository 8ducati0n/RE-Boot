# 2026-05-20 (Sprint 0 D-10 #1): 발표 데모 단계에서는 백엔드를 사용하지 않음.
# `.env.local`을 만들지 말고, 만들더라도 NEXT_PUBLIC_API_URL은 *비워둘 것*.
# 활성화하면 `/api/tutor/chat` 등 Next.js Route Handler가 외부 프록시로 빠짐 (사고 1순위 원인).
# Phase A에서 백엔드 도입 시 next.config.js rewrites와 함께 복원할 것.
#
# NEXT_PUBLIC_API_URL=http://localhost:8000
